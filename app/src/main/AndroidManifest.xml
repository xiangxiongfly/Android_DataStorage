<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".mmkv.MmkvActivity"></activity>
        <activity android:name=".sp.SpActivity" />
        <activity android:name=".room.OtherActivity" />
        <activity
            android:name=".room.RoomActivity"
            android:label="Room" />
        <activity
            android:name=".litepal.LitePalActivity"
            android:label="LitePal" />
        <activity
            android:name=".sqlite.SQLiteActivity"
            android:label="SQLite" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>